import type { SupabaseClient } from '@supabase/supabase-js';

import type { Env } from '../types/env';

export function getSupabaseClient(_env: Env): SupabaseClient | null {
  // Stub for Subfase A. Supabase wiring will be implemented in Subfase B.
  return null;
}
